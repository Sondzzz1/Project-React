import AppRouter from './router';
import './App.css';

/**
 * App.tsx — Minimal root component for the Hospital Management System.
 * All routing logic is centralized in the AppRouter component (router.tsx).
 */
function App() {
    return <AppRouter />;
}

export default App;
